{
  "name": "sqb-connect-pg",
  "description": "SQB Connection plugin for PostgreSQL",
  "version": "3.0.14",
  "author": "Panates Ltd.",
  "contributors": [
    "Eray Hanoglu <e.hanoglu@panates.com>"
  ],
  "license": "MIT",
  "repository": "panates/sqb-connect-pg",
  "main": "lib/index.js",
  "keywords": [
    "javascript",
    "sqb",
    "connect",
    "postgres",
    "database",
    "adapter",
    "interface"
  ],
  "dependencies": {
    "pg": "^7.5.0",
    "pg-cursor": "^1.3.0",
    "putil-waterfall": "^2.0.3",
    "sqb-serializer-pg": "^3.1.2"
  },
  "devDependencies": {
    "babel-eslint": "^10.0.1",
    "eslint": "^5.7.0",
    "eslint-config-google": "^0.10.0",
    "glob": "^7.1.3",
    "nyc": "^13.0.1",
    "mocha": "^5.2.0",
    "sqb": "^3.5.2"
  },
  "peerDependencies": {},
  "engines": {
    "node": ">= 6.0"
  },
  "files": [
    "LICENSE",
    "README.md",
    "lib/"
  ],
  "nyc": {
    "temp-directory": "./coverage/.nyc_output"
  },
  "scripts": {
    "test": "mocha --require ./test/support/env --reporter spec --bail --check-leaks test/",
    "cover": "nyc --reporter html --reporter text npm run test",
    "travis-test": "mocha --require ./test/support/env-travis --require ./test/support/env --reporter spec --check-leaks test/",
    "travis-cover": "nyc --reporter lcovonly npm run travis-test"
  }
}
